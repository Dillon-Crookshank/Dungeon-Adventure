-- CREATE TABLE actorTemplates(
-- name TEXT NOT NULL PRIMARY KEY,
-- hitpoints REAL NOT NULL,
-- attack REAL NOT NULL,
-- defence REAL NOT NULL,
-- mana REAL NOT NULL,
-- initiative INTEGER NOT NULL);

-- INSERT INTO actorTemplates(name, hitpoints, attack, defence, mana, initiative)
-- VALUES ("warrior", 25, 10, 5, 5, 2);

-- DELETE FROM actorTemplates WHERE name="Warrior";